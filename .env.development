# 后端 REST 与 Swagger：http://localhost:8080/swagger-ui/index.html
# 后端启动后同步 OpenAPI 到仓库：npm run api-docs（写入 _api_docs.json）
# 开发时 Vite 将 /api 代理到 http://localhost:8080（vite.config.ts）
# 若需浏览器直连后端（需后端 CORS）：VITE_API_BASE_URL=http://localhost:8080/api
VITE_API_BASE_URL=/api
